{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "description": "This repo eats its own dog food: it consumes the preset it publishes. Without this, the action digests in .github/workflows/ and the tool versions pinned in tooling/validate.sh would rot, and a stale pin is worse than a floating tag.",
  "extends": ["local>ownctrl/supply-chain"],
  "customManagers": [
    {
      "description": "Keep the pinned tool versions in tooling/validate.sh updated",
      "customType": "regex",
      "managerFilePatterns": ["/^tooling/validate\\.sh$/"],
      "matchStrings": [
        "# renovate: datasource=(?<datasource>\\S+) depName=(?<depName>\\S+)\\s+[A-Z_]+_VERSION=\"\\$\\{[A-Z_]+:-(?<currentValue>[^}\"]+)\\}\""
      ]
    }
  ]
}
